{Singleton.getGraph()
        .getAllPeople()
        .map((person, index) => {
          <>
            <p key={index}>{person}</p>
            {/* <View style={styles.CircleShape} /> */}
          </>
        })}
      <Canvas
        style={{ width: "100%", height: "100%", backgroundColor: "black" }}
      />